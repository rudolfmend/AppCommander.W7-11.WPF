<Application x:Class="AppCommander.W7_11.WPF.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:converters="clr-namespace:AppCommander.W7_11.WPF.Converters"
             StartupUri="MainWindow.xaml">

    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Themes/DarkTheme.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <!-- Convertery pre UserMode visibility (musia byť PO MergedDictionaries) -->
            <converters:ModeToVisibilityConverter x:Key="ModeToVisibilityConverter"/>
            <converters:ModeToVisibilityInverseConverter x:Key="ModeToVisibilityInverseConverter"/>
            <converters:ModeToEnabledConverter x:Key="ModeToEnabledConverter"/>

            <!-- Geometria pre checkmark -->
            <Geometry x:Key="Checkmark">F1M10,1.2L4.7,9.1 4.5,9.1 1,5.2 2.3,3.8 4.3,6.1 8.3,0 10,1.2z</Geometry>

            <!-- UX-optimalizované farby -->
            <SolidColorBrush x:Key="PrimaryBlue" Color="#c5e8fc"/>
            <SolidColorBrush x:Key="PrimaryBlueHover" Color="#41c2fa"/>
            <SolidColorBrush x:Key="PrimaryBluePressed" Color="#2599cc"/>

            <SolidColorBrush x:Key="SuccessGreen" Color="#abffe6"/>
            <SolidColorBrush x:Key="WarningOrange" Color="DarkRed"/>
            <SolidColorBrush x:Key="DangerRed" Color="#fc9379"/>

            <SolidColorBrush x:Key="NeutralGray100" Color="#FFFAF9F8"/>
            <SolidColorBrush x:Key="NeutralGray200" Color="#FFF3F2F1"/>
            <SolidColorBrush x:Key="NeutralGray300" Color="#FFEDEBE9"/>
            <SolidColorBrush x:Key="NeutralGray400" Color="#FFE1DFDD"/>
            <SolidColorBrush x:Key="NeutralGray500" Color="#FFD2D0CE"/>
            <SolidColorBrush x:Key="NeutralGray700" Color="#FF605E5C"/>
            <SolidColorBrush x:Key="NeutralGray800" Color="#FF484644"/>
            <SolidColorBrush x:Key="NeutralGray900" Color="#FF323130"/>
            <SolidColorBrush x:Key="GrayBlueColor" Color="#00589e"/>

            <SolidColorBrush x:Key="Background" Color="#fff"/>
            <SolidColorBrush x:Key="Surface" Color="#fdfdfc"/>
            <SolidColorBrush x:Key="OnSurface" Color="#FF323130"/>
            <SolidColorBrush x:Key="OnSurfaceVariant" Color="#FF605E5C"/>

            <!-- Shadows pomocou DropShadowEffect -->
            <DropShadowEffect x:Key="ElevationLow" 
                              BlurRadius="7" 
                              ShadowDepth="7" 
                              Opacity="0.15" 
                              Color="#FF000000"/>

            <DropShadowEffect x:Key="ElevationMedium" 
                              BlurRadius="8" 
                              ShadowDepth="4" 
                              Opacity="0.25" 
                              Color="#FF000000"/>

            <!-- TextBox s fokus animáciou -->
            <Style x:Key="TextBoxStyle" TargetType="TextBox">
                <Setter Property="Background" Value="{DynamicResource Surface}"/>
                <Setter Property="BorderBrush" Value="{DynamicResource NeutralGray500}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="Padding" Value="1"/>
                <Setter Property="MinHeight" Value="34" />
                <Setter Property="FontFamily" Value="Segoe UI"/>
                <Setter Property="FontSize" Value="18"/>
                <Setter Property="Foreground" Value="{DynamicResource GrayBlueColor}" />
                <Setter Property="Cursor" Value="IBeam" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="TextBox">
                            <Border x:Name="border"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="3">
                                <ScrollViewer x:Name="PART_ContentHost" 
                                            Margin="{TemplateBinding Padding}"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource PrimaryBlueHover}"/>
                                    <Setter Property="Foreground" Value="{DynamicResource PrimaryBlueHover}" />
                                </Trigger>
                                <Trigger Property="IsFocused" Value="True">
                                    <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource PrimaryBluePressed}"/>
                                    <Setter TargetName="border" Property="BorderThickness" Value="2"/>
                                    <Setter Property="Foreground" Value="{DynamicResource NeutralGray900}" />
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter Property="Opacity" Value="0.5"/>
                                    <Setter Property="Foreground" Value="{DynamicResource NeutralGray700}" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style x:Key="VerticalToggleButtonStyle" TargetType="Button">
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="BorderThickness" Value="1,0,0,0"/>
                <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}">
                                <ContentPresenter HorizontalAlignment="Center" 
                                    VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="border" Property="Background" 
                                        Value="{DynamicResource HoverBackgroundBrush}"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter TargetName="border" Property="Background" 
                                            Value="{DynamicResource OnSurfaceVariant}"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

        </ResourceDictionary>
    </Application.Resources>
</Application>
