<Window x:Class="AppCommander.W7_11.WPF.WelcomeWizard"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Welcome to Senaro" 
        Height="766" Width="1430"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">

    <Window.Resources>
        <!-- Style for large mode buttons -->
        <Style x:Key="BigModeButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="300"/>
            <Setter Property="Height" Value="200"/>
            <Setter Property="Margin" Value="8"/>
            <Setter Property="Background" Value="{DynamicResource CardBackgroundBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                                <Setter TargetName="border" Property="Background" Value="{DynamicResource HoverBackgroundBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{DynamicResource AccentBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Style for navigation buttons -->
        <Style x:Key="NavButtonStyle" TargetType="Button">
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Background" Value="{DynamicResource AccentBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}" 
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.9"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Background="{DynamicResource WindowBackgroundBrush}" 
            BorderBrush="{DynamicResource BorderBrush}" 
            BorderThickness="1" 
            CornerRadius="12">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border Grid.Row="0" Background="{DynamicResource AccentBrush}" Padding="20">
                <Grid>
                    <StackPanel>
                        <TextBlock Text="✨ Welcome " 
                                   FontSize="24" FontWeight="UltraLight" 
                                   Foreground="White"/>

                        <TextBlock Text="How will you be using Senaro?" 
                                   FontSize="18" FontWeight="SemiBold"
                                   Foreground="{DynamicResource PrimaryTextBrush}"
                                   HorizontalAlignment="Center"
                                   Margin="0,0,0,2"/>
                        <TextBlock x:Name="TxtSubtitle" 
                                   Text="Step 1 of 3 - Select your profile" 
                                   FontSize="14" 
                                   Foreground="#CCFFFFFF"
                                   Margin="0,4,0,0"/>
                    </StackPanel>

                    <!-- Close button -->
                    <Button Content="✕" 
                            HorizontalAlignment="Right" 
                            VerticalAlignment="Top"
                            Background="Transparent"
                            Foreground="White"
                            BorderThickness="0"
                            FontSize="16"
                            Padding="8"
                            Cursor="Hand"
                            Click="BtnClose_Click"/>
                </Grid>
            </Border>

            <!-- Content Area -->
            <Grid Grid.Row="1" Margin="20">


                <!-- App Logo -->
                <Image Source="/Pictures/Senaro-big-background.png"
                           Localization.Comments="App logo"
                       RenderOptions.BitmapScalingMode="HighQuality"
                       Stretch="UniformToFill" Margin="-101,-20,-23,266" />

                <!-- Step 1: Mode Selection -->
                <Grid x:Name="Step1_ModeSelection">
                    <StackPanel VerticalAlignment="Bottom">

                        <WrapPanel HorizontalAlignment="Center">
                            <!-- Accountant -->
                            <Button x:Name="BtnSelectAccountant" 
                                    Style="{StaticResource BigModeButtonStyle}"
                                    Click="BtnSelectAccountant_Click">
                                <StackPanel>
                                    <TextBlock Text="🧾" FontSize="60" HorizontalAlignment="Center"/>
                                    <TextBlock Text="Accountant" FontWeight="SemiBold" 
                                               FontSize="24"
                                               Foreground="{DynamicResource PrimaryTextBrush}"
                                               HorizontalAlignment="Center" Margin="0,8,0,0"/>
                                    <TextBlock Text="Documents" FontSize="16"
                                               Foreground="{DynamicResource SecondaryTextBrush}"
                                               HorizontalAlignment="Center"/>
                                </StackPanel>
                            </Button>

                            <!-- Tester -->
                            <Button x:Name="BtnSelectTester" 
                                    Style="{StaticResource BigModeButtonStyle}"
                                    Click="BtnSelectTester_Click">
                                <StackPanel>
                                    <TextBlock Text="🧪" FontSize="60" HorizontalAlignment="Center"/>
                                    <TextBlock Text="Tester" FontWeight="SemiBold" 
                                               FontSize="24"
                                               Foreground="{DynamicResource PrimaryTextBrush}"
                                               HorizontalAlignment="Center" Margin="0,8,0,0"/>
                                    <TextBlock Text="UI Testing" FontSize="16"
                                               Foreground="{DynamicResource SecondaryTextBrush}"
                                               HorizontalAlignment="Center"/>
                                </StackPanel>
                            </Button>

                            <!-- 
                            -->
                            <Button x:Name="BtnSelectWorker" 
                                    Style="{StaticResource BigModeButtonStyle}"
                                    Click="BtnSelectWorker_Click">
                                <StackPanel>
                                    <TextBlock Text="📝" FontSize="60" HorizontalAlignment="Center"/>
                                    <TextBlock Text="Office Worker" FontWeight="SemiBold"
                                               FontSize="24"
                                               Foreground="{DynamicResource PrimaryTextBrush}"
                                               HorizontalAlignment="Center" Margin="0,8,0,0"/>
                                    <TextBlock Text="Playback" FontSize="16"
                                               Foreground="{DynamicResource SecondaryTextBrush}"
                                               HorizontalAlignment="Center"/>
                                </StackPanel>
                            </Button>

                            <!-- Developer -->
                            <Button x:Name="BtnSelectDeveloper" 
                                    Style="{StaticResource BigModeButtonStyle}"
                                    Click="BtnSelectDeveloper_Click">
                                <StackPanel>
                                    <TextBlock Text="🛠️" FontSize="50" HorizontalAlignment="Center"/>
                                    <TextBlock Text="Developer" FontWeight="SemiBold"
                                               Foreground="{DynamicResource PrimaryTextBrush}"
                                               FontSize="24"
                                               HorizontalAlignment="Center" Margin="0,8,0,0"/>
                                    <TextBlock Text="All Features" FontSize="16"
                                               Foreground="{DynamicResource SecondaryTextBrush}"
                                               HorizontalAlignment="Center"/>
                                </StackPanel>
                            </Button>
                        </WrapPanel>

                        <TextBlock x:Name="TxtModeDescription" 
                                   Text="Select the profile that best describes your work"
                                   FontSize="13"
                                   Foreground="{DynamicResource SecondaryTextBrush}"
                                   HorizontalAlignment="Center"
                                   TextWrapping="Wrap"
                                   TextAlignment="Center"
                                   Margin="0,20,0,0"/>
                    </StackPanel>
                </Grid>

                <!-- Step 2: Mode-specific Settings -->
                <Grid x:Name="Step2_Settings" Visibility="Collapsed">
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" MaxWidth="400">
                        <TextBlock x:Name="TxtStep2Title" 
                                   Text="Settings for your profile" 
                                   FontSize="18" FontWeight="SemiBold"
                                   Foreground="{DynamicResource PrimaryTextBrush}"
                                   HorizontalAlignment="Center"
                                   Margin="0,160,0,20"/>

                        <!-- Accountant Settings -->
                        <StackPanel x:Name="AccountantSettings" Visibility="Collapsed">
                            <CheckBox x:Name="ChkAutoProcess" 
                                      Content="Auto-process new files"
                                      Foreground="{DynamicResource PrimaryTextBrush}"
                                      Margin="0,8"/>
                            <StackPanel Orientation="Horizontal" Margin="0,8">
                                <TextBlock Text="Output format:" VerticalAlignment="Center" Margin="0,0,10,0"
                                           Foreground="{DynamicResource PrimaryTextBrush}"/>
                                <ComboBox x:Name="CmbOutputFormat" Width="120">
                                    <ComboBoxItem Content="Excel" IsSelected="True"/>
                                    <ComboBoxItem Content="CSV"/>
                                </ComboBox>
                            </StackPanel>
                        </StackPanel>

                        <!-- Tester Settings -->
                        <StackPanel x:Name="TesterSettings" Visibility="Collapsed">
                            <CheckBox x:Name="ChkEnableInspector" 
                                      Content="Enable UI Inspector"
                                      Foreground="{DynamicResource PrimaryTextBrush}"
                                      IsChecked="True"
                                      Margin="0,8"/>
                            <CheckBox x:Name="ChkDebugOverlay" 
                                      Content="Enable Debug Overlay"
                                      Foreground="{DynamicResource PrimaryTextBrush}"
                                      Margin="0,8"/>
                            <CheckBox x:Name="ChkRealtimeHighlight" 
                                      Content="Real-time element highlighting"
                                      Foreground="{DynamicResource PrimaryTextBrush}"
                                      IsChecked="True"
                                      Margin="0,8"/>
                        </StackPanel>

                        <!-- Worker Settings -->
                        <StackPanel x:Name="WorkerSettings" Visibility="Collapsed">
                            <CheckBox x:Name="ChkShowRecommended" 
                                      Content="Show recommended sequences"
                                      Foreground="{DynamicResource PrimaryTextBrush}"
                                      IsChecked="True"
                                      Margin="0,8"/>
                        </StackPanel>

                        <!-- Developer Settings -->
                        <StackPanel x:Name="DeveloperSettings" Visibility="Collapsed">
                            <CheckBox x:Name="ChkVerboseLogging" 
                                      Content="Verbose logging"
                                      Foreground="{DynamicResource PrimaryTextBrush}"
                                      Margin="0,8"/>
                            <CheckBox x:Name="ChkDebugModules" 
                                      Content="Enable debug modules"
                                      Foreground="{DynamicResource PrimaryTextBrush}"
                                      IsChecked="True"
                                      Margin="0,8"/>
                            <CheckBox x:Name="ChkAutoDebugConsole" 
                                      Content="Auto-open debug console"
                                      Foreground="{DynamicResource PrimaryTextBrush}"
                                      Margin="0,8"/>
                        </StackPanel>
                    </StackPanel>
                </Grid>

                <!-- Step 3: Finish -->
                <Grid x:Name="Step3_Finish" Visibility="Collapsed">
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                        <TextBlock Text="✅" FontSize="48" Margin="0,250,0,0" HorizontalAlignment="Center"/>
                        <TextBlock Text="All set!" 
                                   FontSize="24" FontWeight="Bold"
                                   Foreground="{DynamicResource PrimaryTextBrush}"
                                   HorizontalAlignment="Center"
                                   Margin="0,10,0,8"/>

                        <TextBlock x:Name="TxtFinishSummary"
                                   Text="Profile: Developer"
                                   FontSize="14"
                                   Foreground="{DynamicResource SecondaryTextBrush}"
                                   HorizontalAlignment="Center"
                                   Margin="0,0,0,4"/>

                        <TextBlock x:Name="TxtFinishFeatures"
                                   Text="Active features: Recording, Playback, Debug"
                                   FontSize="12"
                                   Foreground="{DynamicResource SecondaryTextBrush}"
                                   HorizontalAlignment="Center"
                                   TextWrapping="Wrap"
                                   TextAlignment="Center"
                                   MaxWidth="350"/>

                        <TextBlock Text="You can change your mode anytime in the top-right corner."
                                   FontSize="12"
                                   Foreground="{DynamicResource SecondaryTextBrush}"
                                   HorizontalAlignment="Center"
                                   Margin="0,20,0,0"/>
                    </StackPanel>
                </Grid>
            </Grid>

            <!-- Footer Navigation -->
            <Border Grid.Row="2" 
                    Background="{DynamicResource PanelBackgroundBrush}" 
                    BorderBrush="{DynamicResource BorderBrush}"
                    BorderThickness="0,1,0,0"
                    Padding="20">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <Button x:Name="BtnBack" 
                            Grid.Column="0"
                            Content="← Back" 
                            Style="{StaticResource NavButtonStyle}"
                            Background="{DynamicResource SecondaryButtonBrush}"
                            Click="BtnBack_Click"
                            Visibility="Collapsed"/>

                    <!-- Step indicators -->
                    <StackPanel Grid.Column="1" 
                                Orientation="Horizontal" 
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center">
                        <Ellipse x:Name="Step1Indicator" Width="10" Height="10" Fill="{DynamicResource AccentBrush}" Margin="4"/>
                        <Ellipse x:Name="Step2Indicator" Width="10" Height="10" Fill="{DynamicResource BorderBrush}" Margin="4"/>
                        <Ellipse x:Name="Step3Indicator" Width="10" Height="10" Fill="{DynamicResource BorderBrush}" Margin="4"/>
                    </StackPanel>

                    <Button x:Name="BtnNext" 
                            Grid.Column="2"
                            Content="Next →" 
                            Style="{StaticResource NavButtonStyle}"
                            Click="BtnNext_Click"
                            IsEnabled="False"/>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>
