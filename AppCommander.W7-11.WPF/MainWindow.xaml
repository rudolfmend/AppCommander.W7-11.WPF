<Window x:Class="AppCommander.W7_11.WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="AppCommander" 
        Height="900" Width="1400" 
        MinHeight="600" MinWidth="1200"
        Icon="appcommander_icon.ico"
        WindowStartupLocation="CenterScreen" 
        FontSize="14" 
        FontFamily="Segoe UI"
        Background="#F8F9FA">

    <Window.Resources>
        <!-- Modern Button Styles -->
        <Style x:Key="ModernPrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="#0078D4"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="4"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                BorderBrush="{TemplateBinding BorderBrush}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#106EBE"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#005A9E"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernSecondaryButton" TargetType="Button">
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="Foreground" Value="#323130"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#D1D1D1"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="FontWeight" Value="Normal"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="4"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                BorderBrush="{TemplateBinding BorderBrush}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#F3F2F1"/>
                                <Setter Property="BorderBrush" Value="#C7C7C7"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernDangerButton" TargetType="Button">
            <Setter Property="Background" Value="#D13438"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="4"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                BorderBrush="{TemplateBinding BorderBrush}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#A4262C"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Modern Card Style -->
        <Style x:Key="ModernCard" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#E1E1E1"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect ShadowDepth="2" Direction="270" Color="#E0E0E0" Opacity="0.3" BlurRadius="4"/>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Modern TextBox Style -->
        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#D1D1D1"/>
            <Setter Property="Padding" Value="8"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="#0078D4"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Modern Menu Bar -->
        <Border Grid.Row="0" Background="White" BorderBrush="#E1E1E1" BorderThickness="0,0,0,1">
            <Menu Background="Transparent" Padding="8" >
                <MenuItem Header="_File" Foreground="#323130" Style="{StaticResource TopLevelMenuItem}" >
                    <MenuItem Header="_New Sequence" Click="NewSequence_Click" InputGestureText="Ctrl+N" Style="{StaticResource ModernMenuItem}" />
                    <MenuItem Header="_Open Sequence..." Click="OpenSequence_Click" InputGestureText="Ctrl+O" Style="{StaticResource ModernMenuItem}" >
                    <Separator/>
                        <MenuItem Header="_Save Sequence" Click="SaveSequence_Click" InputGestureText="Ctrl+S" Style="{StaticResource ModernMenuItem}" />
                        <MenuItem Header="Save Sequence _As..." Click="SaveSequenceAs_Click" InputGestureText="Ctrl+Shift+S" Style="{StaticResource ModernMenuItem}" />
                    <Separator/>
                        <MenuItem Header="E_xit" Click="Exit_Click" InputGestureText="Alt+F4" Style="{StaticResource ModernMenuItem}" />
                </MenuItem>
                    <MenuItem Header="_Commands" Foreground="#323130"  Style="{StaticResource TopLevelMenuItem}" >
                        <MenuItem Header="Add _Wait Command..." Click="AddWaitCommand_Click" Style="{StaticResource ModernMenuItem}" />
                        <MenuItem Header="Add _Loop Start" Click="AddLoopStart_Click" Style="{StaticResource ModernMenuItem}" />
                        <MenuItem Header="Add Loop _End" Click="AddLoopEnd_Click" Style="{StaticResource ModernMenuItem}" />
                    <Separator/>
                        <MenuItem Header="_Edit Selected Command..." Click="EditCommand_Click" Style="{StaticResource ModernMenuItem}" />
                        <MenuItem Header="_Delete Selected Command" Click="DeleteCommand_Click" Style="{StaticResource ModernMenuItem}" />
                </MenuItem>
                    <MenuItem Header="_Tools" Foreground="#323130" Style="{StaticResource TopLevelMenuItem}" >
                        <MenuItem Header="_Window Selector" Click="WindowSelector_Click" Style="{StaticResource ModernMenuItem}" />
                        <MenuItem Header="_Element Inspector" Click="ElementInspector_Click" Style="{StaticResource ModernMenuItem}" />
                        <MenuItem Header="_Settings" Click="Settings_Click" Style="{StaticResource ModernMenuItem}" >
                </MenuItem>
                        <MenuItem Header="_Help" Foreground="#323130" Style="{StaticResource TopLevelMenuItem}" >
                            <MenuItem Header="_User Guide" Click="UserGuide_Click" Style="{StaticResource ModernMenuItem}" />
                            <MenuItem Header="_About" Click="About_Click" Style="{StaticResource ModernMenuItem}" />
                </MenuItem>
            </Menu>
        </Border>

        <!-- Modern Toolbar -->
        <Border Grid.Row="1" Background="White" BorderBrush="#E1E1E1" BorderThickness="0,0,0,1" Padding="16">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <!-- Target Selection -->
                <StackPanel Orientation="Horizontal" Margin="0,0,24,0">
                    <TextBlock Text="Target:" VerticalAlignment="Center" Margin="0,0,8,0" 
                               FontWeight="SemiBold" Foreground="#323130"/>
                    <Button Name="btnSelectTargetByClick" Content="🎯 Click to Select" 
                            Style="{StaticResource ModernPrimaryButton}" 
                            Click="SelectTargetByClick_Click" 
                            Margin="0,0,8,0" Height="36"/>
                    <Button Name="btnSelectTarget" Content="📋 Browse" 
                            Style="{StaticResource ModernSecondaryButton}" 
                            Click="SelectTarget_Click" Height="36"/>
                </StackPanel>

                <Separator Margin="0,0,24,0" Style="{x:Null}" Background="#E1E1E1" Width="1"/>

                <!-- Recording Controls -->
                <StackPanel Orientation="Horizontal" Margin="0,0,24,0">
                    <TextBox Name="txtSequenceName" Text="My_Recording" 
                             Style="{StaticResource ModernTextBox}" 
                             Width="180" Height="36" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <Button Name="btnRecording" Content="🔴 Start Recording" 
                            Style="{StaticResource ModernDangerButton}" 
                            Click="ToggleRecording_Click" Height="36" Margin="0,0,8,0"/>
                    <Button Name="btnPlayCommands" Content="▶ Play" Padding="20"
                            Style="{StaticResource ModernPrimaryButton}" 
                            Click="PlaySequence_Click" Height="36"/>
                </StackPanel>

                <Separator Margin="0,0,24,0" Style="{x:Null}" Background="#E1E1E1" Width="1"/>

                <!-- Playback Controls -->
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Repeat:" VerticalAlignment="Center" Margin="0,0,8,0" 
                        FontWeight="SemiBold" Foreground="#323130"/>
                    <TextBox Name="txtRepeatCount" Text="1" 
                         Style="{StaticResource ModernTextBox}"     
                         Width="50" Height="36" TextAlignment="Center" Margin="0,0,8,0"/>
                    <CheckBox Name="chkInfiniteLoop" Content="∞" VerticalAlignment="Center" 
                        Checked="InfiniteLoop_Checked" Unchecked="InfiniteLoop_Unchecked" Margin="0,0,16,0"/>

                    <!-- Playback buttons -->
                    <Button Name="btnPlay" Content="▶ Play" 
                        Style="{StaticResource ModernPrimaryButton}" 
                        Click="PlaySequence_Click" Height="36" Margin="0,0,4,0"/>
                    <Button Name="btnPause" Content="⏸ Pause" 
                        Style="{StaticResource ModernSecondaryButton}" 
                        Click="PausePlayback_Click" Height="36" Margin="0,0,4,0" IsEnabled="False"/>
                    <Button Name="btnStop" Content="⏹ Stop" 
                        Style="{StaticResource ModernSecondaryButton}" 
                        Click="StopPlayback_Click" Height="36" IsEnabled="False"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- Main Content -->
        <Grid Grid.Row="2" Margin="16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="16"/>
                <ColumnDefinition Width="320"/>
            </Grid.ColumnDefinitions>

            <!-- Commands Panel -->
            <Border Grid.Column="0" Style="{StaticResource ModernCard}">
                <Grid Margin="16">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Header -->
                    <Grid Grid.Row="0" Margin="0,0,0,16">
                        <TextBlock Text="Recorded Commands" FontSize="18" FontWeight="SemiBold" 
                                   Foreground="#323130" VerticalAlignment="Center"/>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                            <TextBlock Name="txtCommandCount" Text="0 commands" 
                                       Foreground="#605E5C" VerticalAlignment="Center" FontSize="12"/>
                        </StackPanel>
                    </Grid>

                    <!-- DataGrid -->
                    <DataGrid Name="dgCommands" Grid.Row="1" AutoGenerateColumns="False" 
                              HeadersVisibility="Column" CanUserSortColumns="True"
                              GridLinesVisibility="None" AlternatingRowBackground="#FAFAFA"
                              Background="Transparent" BorderThickness="0" FontSize="13">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="#" Binding="{Binding StepNumber}" Width="50"/>
                            <DataGridTextColumn Header="Element" Binding="{Binding ElementName}" Width="140"/>
                            <DataGridTextColumn Header="Action" Binding="{Binding Type}" Width="100"/>
                            <DataGridTextColumn Header="Value" Binding="{Binding Value}" Width="120"/>
                            <DataGridTextColumn Header="X" Binding="{Binding ElementX}" Width="60"/>
                            <DataGridTextColumn Header="Y" Binding="{Binding ElementY}" Width="60"/>
                            <DataGridTextColumn Header="Time" Binding="{Binding Timestamp, StringFormat='HH:mm:ss'}" Width="80"/>
                        </DataGrid.Columns>
                    </DataGrid>
                    <Separator Margin="0,0,24,0" Style="{x:Null}" Background="#E1E1E1" Width="1"/>

                    <!-- Commands Toolbar -->
                    <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,16,0,0">
                        <Button Content="✏ Edit" Click="EditCommand_Click" 
                                Style="{StaticResource ModernSecondaryButton}" 
                                Margin="0,0,8,0" Height="32" Padding="12,4"/>
                        <Button Content="🗑 Delete" Click="DeleteCommand_Click" 
                                Style="{StaticResource ModernDangerButton}" 
                                Margin="0,0,8,0" Height="32" Padding="12,4"/>
                        <Button Content="▶ Test" Click="TestPlayback_Click" 
                                Style="{StaticResource ModernSecondaryButton}" 
                                Height="32" Padding="12,4"/>
                    </StackPanel>
                </Grid>
            </Border>

            
            <!-- Side Panel -->
            <StackPanel Grid.Column="2">
                <!-- Target Information Card -->
                <ScrollViewer></ScrollViewer>
                <Border Style="{StaticResource ModernCard}" Margin="0,0,0,16">
                    <StackPanel Margin="16">
                        <TextBlock Text="Target Information" FontSize="16" FontWeight="SemiBold" 
                                   Foreground="#323130" Margin="0,0,0,12"/>

                        <!-- Selection Status -->
                        <Border Name="selectionModeIndicator" 
                                Background="#FFF4CE" BorderBrush="#F7C52D" BorderThickness="1" 
                                CornerRadius="4" Padding="8" Margin="0,0,0,12" Visibility="Collapsed">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="🎯" Margin="0,0,6,0"/>
                                <TextBlock Name="txtSelectionMode" Text="Click Selection Active" 
                                           FontWeight="SemiBold" Foreground="#8A6914"/>
                            </StackPanel>
                        </Border>

                        <!-- Target Details -->
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Row="0" Grid.Column="0" Text="Window:" 
                                       FontWeight="SemiBold" Margin="0,0,8,4" Foreground="#323130"/>
                            <TextBlock Name="lblTargetWindow" Grid.Row="0" Grid.Column="1" 
                                       Text="No target selected" Foreground="#605E5C" 
                                       TextWrapping="Wrap" Margin="0,0,0,4"/>

                            <TextBlock Grid.Row="1" Grid.Column="0" Text="Process:" 
                                       FontWeight="SemiBold" Margin="0,0,8,4" Foreground="#323130"/>
                            <TextBlock Name="txtTargetProcess" Grid.Row="1" Grid.Column="1" 
                                       Text="-" Foreground="#605E5C" Margin="0,0,0,4"/>
                        </Grid>

                        <!-- Quick Actions -->
                        <StackPanel Orientation="Horizontal" Margin="0,12,0,0">
                            <Button Name="btnQuickReselect" Content="🔄 Reselect" 
                                    Click="SelectTargetByClick_Click" 
                                    Style="{StaticResource ModernSecondaryButton}" 
                                    Height="32" Padding="8,4" Margin="0,0,8,0" FontSize="12"/>
                            <Button Name="btnQuickBrowse" Content="📋 Browse" 
                                    Click="SelectTarget_Click" 
                                    Style="{StaticResource ModernSecondaryButton}" 
                                    Height="32" Padding="8,4" FontSize="12"/>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!-- Status Card -->
                <Border Style="{StaticResource ModernCard}" Margin="0,0,0,16">
                    <StackPanel Margin="16">
                        <TextBlock Text="System Status" FontSize="16" FontWeight="SemiBold" 
                   Foreground="#323130" Margin="0,0,0,12"/>

                        <StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                <Ellipse Width="8" Height="8" Fill="#D13438" Margin="0,0,8,0" VerticalAlignment="Center"/>
                                <TextBlock Name="lblAutoDetectionStatus" Text="Auto-Detection Inactive" 
                           Foreground="#605E5C" FontSize="12"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                <Ellipse Width="8" Height="8" Fill="#D13438" Margin="0,0,8,0" VerticalAlignment="Center"/>
                                <TextBlock Name="lblUIRecordingStatus" Text="UI Scanning Inactive" 
                           Foreground="#605E5C" FontSize="12"/>
                            </StackPanel>
                        </StackPanel>

                        <!-- Auto Mode Toggle -->
                        <Button Name="btnToggleAutoMode" Content="🎯 Auto Mode OFF" 
                Click="ToggleAutomaticMode_Click" 
                Style="{StaticResource ModernSecondaryButton}" 
                HorizontalAlignment="Stretch" Height="32" Margin="0,8,0,0"/>

                        <ProgressBar Name="progressEnhancedRecording" Height="4" 
                     Background="#F3F2F1" Foreground="#0078D4"
                     IsIndeterminate="False" Visibility="Collapsed" Margin="0,8,0,0"/>
                    </StackPanel>
                </Border>
                
                <!-- Quick Actions Card -->
                <Border Style="{StaticResource ModernCard}">
                    <StackPanel Margin="16">
                        <TextBlock Text="Quick Actions" FontSize="16" FontWeight="SemiBold" 
                                   Foreground="#323130" Margin="0,0,0,12"/>

                        <StackPanel>
                            <Button Content="🔍 Element Inspector" Click="ElementInspector_Click" 
                                    Style="{StaticResource ModernSecondaryButton}" 
                                    HorizontalAlignment="Stretch" Height="36" Margin="0,0,0,8"/>
                            <Button Content="⚙ Settings" Click="Settings_Click" 
                                    Style="{StaticResource ModernSecondaryButton}" 
                                    HorizontalAlignment="Stretch" Height="36" Margin="0,0,0,8"/>
                            <Button Content="📊 Debug Info" Click="ExportSequenceForDebug_Click" 
                                    Style="{StaticResource ModernSecondaryButton}" 
                                    HorizontalAlignment="Stretch" Height="36"/>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <Border Style="{StaticResource ModernCard}" Margin="0,0,0,16">
                    <StackPanel Margin="16">
                        <TextBlock Text="Element Statistics" FontSize="16" FontWeight="SemiBold" 
                   Foreground="#323130" Margin="0,0,0,12"/>

                        <ListBox Name="lstElementStats" Height="120" Background="Transparent" 
                                BorderThickness="0" FontSize="12">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Margin="0,2">
                                        <TextBlock Text="{Binding ElementName}" FontWeight="SemiBold" Foreground="#323130"/>
                                        <TextBlock Text="{Binding UsageCount, StringFormat='Used: {0} times'}" 
                                   FontSize="11" Foreground="#605E5C"/>
                                    </StackPanel>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>

                        <Button Content="↻ Refresh Stats" Click="AutoRefreshAllUIElements_Click" 
                Style="{StaticResource ModernSecondaryButton}" 
                HorizontalAlignment="Stretch" Height="32" Margin="0,8,0,0"/>
                    </StackPanel>
                </Border>
            </StackPanel>
            
        </Grid>

        <!-- Modern Status Bar -->
        <Border Grid.Row="3" Background="White" BorderBrush="#E1E1E1" BorderThickness="0,1,0,0" 
                Padding="16,8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Name="txtStatusBar" Grid.Column="0" 
                           Text="Ready" FontWeight="SemiBold" Foreground="#0078D4"/>

                <TextBlock Name="txtTarget" Grid.Column="1" 
                           Text="No target selected" Foreground="#605E5C" 
                           Margin="16,0"/>

                <TextBlock Name="lblStatusBarRecording" Grid.Column="2" 
                           Text="Not Recording" Foreground="#605E5C"/>
            </Grid>
        </Border>
    </Grid>
</Window>