<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- High Contrast Theme Color Palette -->
    <Color x:Key="PrimaryColor">#00FFFF</Color>
    <Color x:Key="SecondaryColor">#0080FF</Color>
    <Color x:Key="AccentColor">#FFFF00</Color>

    <!-- Background Colors -->
    <Color x:Key="WindowBackgroundColor">#000000</Color>
    <Color x:Key="PanelBackgroundColor">#000000</Color>
    <Color x:Key="CardBackgroundColor">#000000</Color>
    <Color x:Key="HoverBackgroundColor">#333333</Color>

    <!-- Text Colors -->
    <Color x:Key="PrimaryTextColor">#FFFFFF</Color>
    <Color x:Key="SecondaryTextColor">#CCCCCC</Color>
    <Color x:Key="AccentTextColor">#00FFFF</Color>
    <Color x:Key="DisabledTextColor">#808080</Color>

    <!-- Border Colors -->
    <Color x:Key="BorderColor">#FFFFFF</Color>
    <Color x:Key="FocusBorderColor">#FFFF00</Color>
    <Color x:Key="HoverBorderColor">#00FFFF</Color>

    <!-- Status Colors -->
    <Color x:Key="SuccessColor">#00FF00</Color>
    <Color x:Key="WarningColor">#FFFF00</Color>
    <Color x:Key="ErrorColor">#FF0000</Color>
    <Color x:Key="InfoColor">#00FFFF</Color>

    <!-- Brushes -->
    <SolidColorBrush x:Key="PrimaryBrush" Color="{StaticResource PrimaryColor}"/>
    <SolidColorBrush x:Key="SecondaryBrush" Color="{StaticResource SecondaryColor}"/>
    <SolidColorBrush x:Key="AccentBrush" Color="{StaticResource AccentColor}"/>

    <SolidColorBrush x:Key="WindowBackgroundBrush" Color="{StaticResource WindowBackgroundColor}"/>
    <SolidColorBrush x:Key="PanelBackgroundBrush" Color="{StaticResource PanelBackgroundColor}"/>
    <SolidColorBrush x:Key="CardBackgroundBrush" Color="{StaticResource CardBackgroundColor}"/>
    <SolidColorBrush x:Key="HoverBackgroundBrush" Color="{StaticResource HoverBackgroundColor}"/>

    <SolidColorBrush x:Key="PrimaryTextBrush" Color="{StaticResource PrimaryTextColor}"/>
    <SolidColorBrush x:Key="SecondaryTextBrush" Color="{StaticResource SecondaryTextColor}"/>
    <SolidColorBrush x:Key="AccentTextBrush" Color="{StaticResource AccentTextColor}"/>
    <SolidColorBrush x:Key="DisabledTextBrush" Color="{StaticResource DisabledTextColor}"/>

    <SolidColorBrush x:Key="BorderBrush" Color="{StaticResource BorderColor}"/>
    <SolidColorBrush x:Key="FocusBorderBrush" Color="{StaticResource FocusBorderColor}"/>
    <SolidColorBrush x:Key="HoverBorderBrush" Color="{StaticResource HoverBorderColor}"/>

    <SolidColorBrush x:Key="SuccessBrush" Color="{StaticResource SuccessColor}"/>
    <SolidColorBrush x:Key="WarningBrush" Color="{StaticResource WarningColor}"/>
    <SolidColorBrush x:Key="ErrorBrush" Color="{StaticResource ErrorColor}"/>
    <SolidColorBrush x:Key="InfoBrush" Color="{StaticResource InfoColor}"/>

    <!-- ODSTRÁNENÉ: Všetky Button style definície -->
    <!-- Necháme App.xaml aby riadil button štýly -->
    <!-- High contrast téma iba predefinuje farby, nie kontroly -->

</ResourceDictionary>
    